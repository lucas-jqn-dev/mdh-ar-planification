export const environment = {
  production: true,
  // Relativo a propósito: en producción Nest sirve este build y la API
  // desde el mismo origen (ver ServeStaticModule en app.module.ts).
  apiUrl: '/api',
};
